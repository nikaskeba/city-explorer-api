{
  "name": "your-api-name",
  "version": "1.0.0",
  "scripts": {
    "start": "netlify-lambda serve src/functions"
  },
  "dependencies": {
    "express": "your-version",
    "serverless-http": "your-version"
  }
}
